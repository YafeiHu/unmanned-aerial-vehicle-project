/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package business;


import java.util.ArrayList;
import java.util.Timer;
import java.util.TimerTask;

/**
 *
 * @author huyafei
 */
public class DecisionAgent  {
/*
    private Graph graph;
    private Vertex vetex;
    ArrayList<Vertex> result=new ArrayList<>();
    public void getData(Graph graph) {
        Timer timer = new Timer();
        WeatherAgent weatherAgent = new WeatherAgent();
        weatherAgent.getData(graph);
        timer.schedule(weatherAgent, 1000, 1000);
        this.graph=weatherAgent.getChangedGraph();
    } 
    
    @Override
    public void run() {
        // result=graph.Dijkstra("a","c");
         vetex=result.get(0);
    }

    public Graph getGraph() {
        return graph;
    }

    public void setGraph(Graph graph) {
        this.graph = graph;
    }

    public ArrayList<Vertex> getResult() {
        return result;
    }

    public void setResult(ArrayList<Vertex> result) {
        this.result = result;
    }

    public Vertex getVetex() {
        return vetex;
    }

    public void setVetex(Vertex vetex) {
        this.vetex = vetex;
    }
    */
}
